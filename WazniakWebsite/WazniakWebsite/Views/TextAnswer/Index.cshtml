@model IEnumerable<WazniakWebsite.Models.TextAnswer>

@{
    ViewBag.Title = "Index";
}

<h2 class="PurpleText">Text Answers</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table table-striped lightPurpleBackground">
    <tr class="PurpleText">
        <th>
            @Html.DisplayNameFor(model => model.Text)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Text)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.TaskID }, new { @class = "PurpleText" }) |
                @Html.ActionLink("Details", "Details", new { id = item.TaskID }, new { @class = "PurpleText" }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.TaskID }, new { @class = "PurpleText" })
            </td>
        </tr>
    }

</table>
